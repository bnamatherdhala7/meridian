sp:
  mode: mock  # set to "live" with SP_ENDPOINT + SP_TOKEN env vars
  endpoint: "${SP_ENDPOINT}"
  token: "${SP_TOKEN}"

claude:
  model: claude-sonnet-4-6
  max_tokens: 4096

agent:
  max_tool_calls: 10
  confidence_threshold: 0.75
